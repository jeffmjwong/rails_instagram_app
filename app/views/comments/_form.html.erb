<%= form_for [@instapost, comment] do |f| %>

  <div class="field">
    <%= f.label :content, "Comment" %>
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
